services:
  db:
    image: mariadb:latest
    container_name: mcd_db
    restart: always
    environment:
      MARIADB_DATABASE: SAE_db
      MARIADB_ROOT_PASSWORD: root
    ports:
      - "3306:3306"